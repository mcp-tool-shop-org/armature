# E09 — closing ruling: the clean chain is the studio's baseline

**E09 is CLOSED. Advisor ruling 2026-08-12; the Director approved the baseline by eye the
same day** — the lifted review clip and the Gate 0 sheet, judged at his zoom; his eye is the
verifier of record and it has ruled. Merged to `main` at `3728a59` (8,964 insertions: the
chain's tooling, its tests, and the full report). The worktree `E:\AI\armature-E09` is
**retained** — the outputs (sheets, lossless frames, review clips, the lifted GLB) live
there git-ignored by design, pinned by hash in the report's artifact tables.

## What the baseline is

- **The solver, exact:** position and rotation round-trips < 1e-9, idempotent, gates
  exercised broken under `-O`; twist recovered only under a stated hinge assumption, with
  per-frame conditioning reported (never a boolean).
- **The floor, decomposed (B1):** 7.37° representational · 34.87° deployable · 31.17° after
  the one recorded EMA lever; the axis offset lands on the root; **arms worst in every
  arm** — the finding that ordered every later inspection.
- **The chain on its true population (A3):** one generation, reference-documented settings,
  donor gate passed (mean 2.0176 — thin, recorded, clause raised forward by A4), 65/65
  detection with heels observed, lift → solve → the rig performing the dance. Reserve
  unspent; both meters reported.
- **Instruments adopted pipeline-wide:** Gate ROUTE (`route_gates.py`, walks subgraph
  blueprints), the donor acceptance gate (`donor_gate.py`), the saved-file round-trip
  admission (`gate_saved_graph.py`), the ordered fetch + order discriminator
  (`fetch_t2v_run.py`).

## Laws carried out of E09

The served-template law (in CLAUDE.md, measured twice). Gates that fire on their own
authors are the only gates worth having — the widget-table hole and the negative-prompt
citation catch were both self-inflicted and self-caught. And look-then-describe, measured
twice more inside one experiment: the executor's contact-sheet tilt misread, self-caught by
measuring then looking at full size; the advisor's probe narration, corrected the harder
way. The record keeps both.

## The movement library — direction recorded (the Director, 2026-08-12)

A library of performances for the character, grown over time. Two admission paths, one
gate:

1. **Online sources** (marketplace mocap, free collections) enter **only on fetched
   terms** — the AMASS lesson binds: a data licence reaches through everything trained or
   derived from it, so every source gets a licence-map row before its first clip is
   downloaded. Sourcing survey = a future advisor fetch arc, on request.
2. **The owned factory:** the calibrated chain itself. Every licence-clean donor — starting
   with self-generated T2V clips above the A4 gate — lifted onto the rig becomes a library
   entry the studio owns outright: motion record + GLB action + provenance, publishable by
   construction. The library grows as a side effect of making shots.

## Open items carried forward

Aspect ratio / figure size — **the first variable experiment** (E10 candidate; hands 9.3 px
is the number it moves) · `make_lift_sheet` camera-claim fix (with a test) · a foot
instrument valid on hip-origin motion · the axis-offset lever (parked on its second-fixture
condition) · settings-vs-prompt attribution (one generation, only if it ever matters) · the
brush pass (the painted-shot reference set) · retire `E08-run` after verifying its bank is
fully subsumed by this merge.

**Next: E08 — the first painted shot.** The rewrite is appended to its spec; the chain this
experiment calibrated is its floor.
