# E11 — probe ruling: the frame anchored the subject and surrendered the world

**Advisor ruling, 2026-08-12. E11 remains OPEN** — pending the Director's eye on identity
(H-E11a's verdict is his) and the true-tempo A/B (H-E11b), and his pick for the two unspent
reserves. **Verified by this seat before ruling:** the suite (**749 passed / 46 skipped**,
own run); branch `E11-nocontrol` pushed at `f731539`; the Gate 0 sheet and face zoom
examined at full size — the report's descriptions corroborate frame for frame, including
the feet cut by the bottom border from f032.

## R1 — the identity observation stands as described, and his eye rules

With **no reference image, no clip-vision embedding, and no driving signal** — verified in
code, not by reading node order — the character's features persist through the final frame:
bald head, drawn brow, eye/nose/mouth, mitten hands, visible ball joints. The drift axes
his eye should land on are named: surface reads matte→glazed across the clip, hue walks
terracotta→cream in a warm room, the brow reads heavier by f048. If his eye confirms
identity held, this **re-prices the identity question for the free route**: single-frame
subject anchoring measured stronger than the reference-image floor the grounding budgeted
(different mechanism than G13's — start-frame persistence, not reference binding — and it
was the route's named headline risk).

## R2 — the world-replacement finding, folded as a one-seed hypothesis

The authored studio was gone by frame 8 and a complete bar arrived — counter, bottles,
floor, **at least four people past the same crowd-suppressing negative** — from a start
frame containing none of it. Start-frame correlation +1.000 → below 0.5 by f6 → −0.198 at
f64. The executor's four scene predictions missed **in the same direction, registered
blind**: it predicted the frame would anchor the world; instead **the frame anchored the
subject and was discarded as a constraint on the world.** Under the two-seed rule this is
an observation, not a route property — but as a hypothesis it is the most product-shaped
finding since E08: *author the character exactly; let text own the world.* One reserve
spent on a second seed would promote or kill it.

## R3 — the route's price is measured, and the shelf already holds its lever

A large uncommanded push-in: whole figure through f024, feet cut from f032, the horizon
found on 4 of 65 frames and never again. Consult #7's ruling landed the exact counter while
this branch was open: **`WanCameraEmbedding` → `WanCameraImageToVideo`** — core nodes,
Fun-Camera weights fetched Apache — whose `Static` pose plus `speed` exists to hold a
camera. **Wave 2, if the Director picks it: the same probe plus the camera embedding, one
variable, one reserve** — simultaneously the discriminating test of the shelf's licensed
lever and the fix for this route's one measured defect.

## R4 — motion, measured beside both siblings

65/65 frames distinct; median per-frame delta 5.282 vs E08's 3.917 — the free route moves
**more** per frame, not less. Exposure is the steadiest of the three probes: |Δ luma|
median 0.210 vs E08's 0.870 vs E10's 9.05. Livelier-or-not is the Director's A/B call, and
the numbers will sit beside whichever way his eye goes.

## R5 — instruments and the report's own catches

`make_gate0_sheet.py` still carries E02-era literals — the **third sighting** of the
stale-label species. Ruled: the fix is **commissioned** — provenance panels derive from
payload records, with a test that proves the panel prints this experiment's values — riding
the next commit that touches any sheet tool. The executor built the no-control sheet fresh
rather than reuse the trap: correct. Its two self-catches (camera numbers quoted before
being read, corrected against provenance and recorded rather than deleted) are the
look-and-verify laws holding inside a report, again.

## R6 — standing

Meters: `estimate_credits` 0; 1 of 3 spent, two reserves whole; GPU bucket to the ledger
when the window resolves. E11 stays open on the Director's verdicts and his pick among:
**(a) wave 2 — camera-hold via the embedding** (this ruling's recommendation: one variable,
fixes the measured price, tests the licensed lever); **(b) arm A2 — the VACE reference
comparison**; **(c) a second seed** for the world-replacement hypothesis; **(d) close as
probed.** The pick is his; the reserves keep.
