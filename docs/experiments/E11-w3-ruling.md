# E11 wave 3 — ruling: the correction proved itself, and the gates grew the organ they were missing

**Advisor ruling, 2026-08-12. E11's ceiling is exhausted (3 of 3); the experiment stands
ready to close on the Director's wave-3 verdicts.** Verified by this seat: the suite
(**827 passed / 46 skipped**, own run); tip `f050a05` pushed; the arc strip examined at
full size — figure coherent f0→f80, the authored studio persisting in every tile, the
horizon visibly level, the f40 right-hand smear where the report puts it. Corroborated.

## R1 — the correction stands, and Gate PAIR is adopted pipeline-wide

The camera claim is solid on instruments a resolution change cannot distort: horizon row
183→182→183 across 81 frames — total spread **one pixel**, found on 70/81 — correlation
with f0 never below 0.9151. Wave 2's collapse cause is confirmed by its mirror: same
conditioning, right weights, coherent clip. **Gate PAIR** (conditioning class → required
weight family, raising in-tool) shipped first as commissioned, went red on the banked
wave-2 graph — and then immediately went red on **seven of the repo's own tests**, whose
helper had encoded wave 2's defect as fixture substrate. That catch, plus the companion
test proving every other gate stays green on the collapsed graph, is the strongest form of
a gate's necessity proof. The `tests/fixtures/` convention (failures stay in the repo,
runnable, as test subjects) is adopted with it.

## R2 — the frame derivation is the exact-variant law working at the resolution layer

Waves 1–2's 832×480 was the plain I2V model's document — a sibling's constraint, inherited.
The Fun-Camera card's verbatim tiers land only at 768: **1024×576**, zero rounding, both
dims ÷16, area exactly 768². The generation-frames law already said "record the constraint
per model in the spec that first uses it"; wave 3 executed it properly, and the record now
carries a measured instance of why: a resolution is a per-model documented fact, never an
inheritance.

## R3 — the alpha law's first implementation is adopted

The canonical renderer's hard-coded `film_transparent = False` — the baked void itself —
is gone. The RGBA master is authored and looked at; **Gate ALPHA** raises on an RGBA file
with no actual transparency (a check that can fail, by construction); the submitted
composite is a named, recorded choice; the provenance block no longer hard-codes the grey
studio as a fact about the world.

## R4 — the two world-behaviors, held honestly at one seed each

Wave 1 (plain I2V): the start frame's world **replaced** wholesale. Wave 3 (Fun-Camera
control tier): the authored world **held** to the last frame — no bar, no people, horizon
unmoved. Six registered deltas separate the waves; three live candidates (a control tier
trained to hold its input; a prompt that no longer names a crowd; a start frame that no
longer contradicts the text). **Attributed to none; one seed each; the two-seed rule
governs.** If both behaviors replicate, the route owns a real dial — *world-from-text* vs
*world-as-authored* — chosen per shot. That is product capability pending replication, and
nothing more tonight.

## R5 — hands: prompt-side mitigation is now measured insufficient

Wave 1 produced the closed claw against Wan's five default deformity negatives; wave 3
produced digits→wisps→stump against the extended surgery. The failure mode moved; the
failure did not. **The geometry lever is promoted**: the F-series hand-mesh item on the
finish ledger is the named fix, and prompt-only hand treatment is recorded as tried-twice.
Whether wave 3's hands beat wave 1's claw is the Director's verdict, not this ruling's.

## R6 — named, not dispatched

The one ASSUMED premise left open: this graph ran cfg 3.5 / euler while the catalog
recommends **cfg 6.0 / uni_pc for these exact files** — the natural single-variable
follow-up if the Director extends the ceiling. E11's three generations are spent;
extension is his word, not this seat's.

## Standing

Meters: `estimate_credits` 0 throughout E11; 3 of 3 spent; GPU buckets to the ledger on
resolution. The true-tempo A/B and the face and hands strips are with the Director. On his
three verdicts — the dance, the camera, the hands — E11 closes and merges, and its capital
is already banked either way: Gate PAIR, Gate ALPHA, the fixtures convention, the derived
frame, and two world-behaviors waiting on their second seeds.
