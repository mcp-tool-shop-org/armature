# E11 — closing ruling: wave 3 is a hard fail at the Director's eye, and the experiment closes

**E11 is CLOSED. The Director's verdict on wave 3, 2026-08-12, recorded as rendered: a hard
fail.** Much worse than wave 1 as a shot — the bar is gone (the frame is the authored
white-and-brown void, held faithfully), the legs read fine enough, and the arms blur and
deform through the turns. The verdict is the judgment of record, and this ruling does not
grade the shot as relative improvement — that error is in the seat error table already, and
it is not repeated here. **Wave 3 fails as a shot, full stop.**

## What the verdict teaches, said plainly

The measurement and the verdict are both true, and together they say one thing: **the
camera tier held exactly what it was handed, and it was handed a void.** Wave 1's plain-I2V
tier invented a bar and lost the camera; wave 3's control tier held the camera and
faithfully preserved a world that was never worth holding — because the start frame's world
was the previz plate, not a scene. "World-as-authored" requires **authoring a world.** My
wave-3 ruling framed the persistence finding with a win's tone; the Director's eye read the
same fact as the shot losing its bar. Metrics are diagnostics; his eye is the judge; the
framing slant is owned here.

## The two levers the verdict points at (named, costed, NOT dispatched — the ceiling is spent)

1. **A scene-bearing start frame on the camera tier.** The alpha law already provides the
   mechanism: the character is authored RGBA, and the submitted composite is a deliberate
   choice — so composite him onto a **real scene plate** (a bar plate, authored or
   generated-and-curated under the gate) instead of the previz void. The tier that holds
   what it is handed then holds a bar. One generation under a fresh ceiling.
2. **The cfg/sampler correction for the arm deformation.** Wave 3 ran cfg 3.5 / euler
   against the catalog's recommendation of **cfg 6.0 / uni_pc for these exact files** — the
   one premise the executor marked ASSUMED and left honestly open. Under-guided sampling is
   a live mechanistic candidate for limbs smearing through fast turns. One generation, one
   variable, same fresh ceiling.

Both levers together are two generations. Whether either runs, and under what experiment
number, is the Director's word.

## The clause verdicts, for the record

Scene: **fail** (the void held). Motion: **fail on the arms** (blur and deformation through
turns; legs acceptable). Camera: not named in the verdict; the one-pixel hold stands as a
measurement. Identity: not named; the face strip stands as a measurement. Hands: subsumed —
the arm deformation dominates the limb verdict, and the geometry lever (F-series hand mesh)
remains promoted from the wave-3 ruling.

## What stands as capital regardless of the verdict

Gate PAIR (with its red test on the banked collapse graph, and its catch of seven
wrong-substrate fixtures) · Gate ALPHA and the alpha law's first implementation · the
fixtures convention · the derived-frame discipline (a resolution is a per-model documented
fact) · the banked Fun-Camera licence row with its wiring warning · the two world-behaviors,
one seed each, awaiting second seeds if ever wanted. Instruments are not shots; they are
listed last deliberately.

## Standing

`E11-nocontrol` merges to `main` at close. E11's final meters: 3 of 3 generations,
`estimate_credits` 0 throughout, GPU buckets to the ledger on resolution. The no-control
route's honest state after three waves: identity holds unanchored (wave 1, his eye); the
free tier paints worlds but wanders (wave 1); the camera tier obeys but must be handed a
world (wave 3); arms/hands need the sampler correction measured and the hand geometry
built. The skeletal route stands parked and unaffected. The next word is the Director's.
