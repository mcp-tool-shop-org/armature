# E13 — halt ruling: the composed route stops one layer before its first credit

**Advisor ruling, 2026-08-13, on the executor's halt report (branch `E13-run` @ `da2849e`,
unmerged — it stays unmerged until E13 closes or is withdrawn; this ruling lands on `main`
and cites it).** Zero credits spent, zero generations, zero uploads accepted. The Director
has not yet picked the path forward; R7 frames that decision and this ruling holds it open.

## R1 — The halt is correct, and the discipline held

The ordered first act ran before anything else: the fresh re-estimate reads 106–211
credits per generation → 424–844 for four, inside the spec's own bracket, under the
900-credit halt — and no `0`-reading meter artifact appeared on this path, which is
itself a data point against the E12 meter anomaly being universal. The A2 pin verified
sha-exact. The node-contract premise re-measured byte-consistent. Then two premises
failed structurally, and the executor stopped without adapting around either, declined
to teach the round-trip table — a green row for an untravelled path would mark it
walked — and recorded the gate gaps instead of letting them pass vacuously. Predictions
were committed before any look at generation output, with blindness disclosed. This is
the posture the repo pays for, executed clean.

## R2 — Finding 1 ruled, and narrowed by a ruling-time measurement

**The A2 path is dead as specced:** no direct video upload exists on this API surface.
The executor re-measured E02's wall today — the same `validation.input` error_type on
both containers, `LoadVideo`'s option list empty, and no URL- or path-based loader
among 124 VIDEO-output classes. That enumeration covered *loaders*. Measured at ruling
time by this seat, same day: **the catalog serves a constructor** — core `CreateVideo`
("Create a video from images"), `images` IMAGE + `fps` FLOAT (1–120, default 30),
optional audio and bit_depth, → VIDEO; `api_node: false`, so it executes as ordinary
workflow compute, not a partner call. The rescue shape E02 pioneered for an IMAGE
socket extends in principle to a VIDEO socket:

```
uploaded PNG frames → the E02 batch mechanism → CreateVideo(fps=16) → reference_videos.video1
```

**The A2 arm is therefore blocked-as-specced, candidate-rescued — not dead.** Unmeasured
links, each a premise for any re-arming: the E02 batch mechanism at 81 frames feeding
`CreateVideo`; the r2v node's runtime acceptance of a constructed VIDEO (typed-compatible,
never executed); upload practicality at 81 content-addressed frames. The r2v node's
`output_node: false` (executor's observation) stands with it — the graph supplies its own
save class. All of this is catalog-and-typing evidence, zero credits spent obtaining it.

## R3 — Finding 2 ruled: the A1 premise failed on a row nobody wrote, and the miss is this seat's

The spec's A1 row — "facet's canonical turnaround renders — MEASURED (facet canon
exists)" — was measured for **existence**, not for **subject**. The dispatch amendment
then pinned an A2 clip whose subject is the wooden lay-figure performer, while facet's
canon turnarounds are the bearded-warrior twin, front and back only, barred from
projection by facet's own MANIFEST. The executor established the mismatch the right way:
frames extracted, everything looked at full size.

**Nobody's premise row carried the conjunction — same character across both arms** — and
an identity probe whose arms disagree about the identity has no experiment left in it.
The repo already holds "predict each clause of a conjunction separately"; this ruling
adds the reciprocal: **a premise table that names two inputs carries a row for their
coherence.** Owned by the advisor seat: the dispatch checklist verified the clip's hash
and never asked whose face was in it. E13's premise table gains the coherence row
retroactively by this ruling; every future multi-reference spec carries one from birth.

## R4 — The performer's real turnarounds are the asset gap: two defects, differently owned

The 8-view set the executor located (`turn_final/`, with `turn_clay/` and the sheet
beside it) fails twice:

1. **Flat-255 alpha over a baked grey void, all eight views** — the authored-RGBA law
   violation (the Director's ruling, 2026-08-12; the same disease standing suspect for
   E08's washed bands). Armature-side, and fixable: the performer GLBs and
   `stage_render.py` are on the rig; a re-render authors true alpha.
2. **White texture-projection holes on six of eight views** — worst on exactly the
   three-quarter and profile the spec names at minimum; views 0 (front) and 4 (back)
   measured clean. A texture defect that **no re-render fixes** — it is facet's
   projection-coverage arc, or a view-selection constraint until that arc runs.

These gate any reference use of this set, not only E13's. Gate LOOK fires on these at
composite; nothing that looks like this is submitted anywhere.

## R5 — What may not proceed

Stills-only with the defective set. References carrying unpainted holes and a baked void
confound the very thing the probe measures — a model painting holes as costume is
indistinguishable from identity failure. No arm runs until its references pass a
full-size look.

## R6 — Instruments and record items owed off this halt

- **`SEED_NODES` and Gate L do not speak this tier's language** — the seed table knows
  only the two KSampler classes (Gate S would report "zero seeds, all pinned" having
  checked nothing), and Gate L's `wan` rules describe pixel dimensions and 4n+1 frames
  where this tier constrains by resolution enum and integer seconds. Both are currently
  checks that cannot fail here. They are taught, with tests riding the commit, **by the
  first spec that arms this tier** — the executor's refusal to record vacuous greens is
  ruled correct.
- **The worktree suite skew** — 970 passed / 48 skipped in the worktree against
  1005 / 13 on `main`, identical 1018 collected — means 35 rig-local tests skip only in
  worktrees. A suite whose skip-set depends on checkout location is reporting
  environment, not code. Owed a look; not urgent; flagged exactly as the executor left
  it (not asserted benign).
- The re-estimate receipt (106–211/generation, this path) rides the credits ledger as
  the tier's first live pricing signal.

## R7 — The decision before the Director, framed with this seat's recommendation

1. **HOLD E13 behind asset repair** — re-render the performer turnaround RGBA-true in
   armature (wanted independently of E13: the flat-alpha set violates a standing ruling
   wherever it is used); look at the result at full size; route the texture-hole
   question to facet's polish arc or constrain views to the measured-clean pair; measure
   the R2 constructor chain at zero credits; then re-arm E13 by amendment with whichever
   arms the assets support.
2. **Re-scope to stills-only now** — ruled out as-is by R5; collapses into shape 1 the
   moment references must be re-made anyway.
3. **Withdraw from the tier** — clean (zero spent, terms work banked for any future
   wan2.x use), but leaves the tier's one product question unanswered.

**Recommendation: shape 1.** Every prerequisite is work the studio wants regardless of
E13 — the RGBA re-render serves all future reference use, the texture holes belong to
facet's own arc, and the chain measurement is free. E13 waits behind assets it would
have been wrong to run without; the waiting is the finding operating as designed.
