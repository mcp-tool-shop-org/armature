# E10 — closing ruling: density adopted, on E08's shoulders

**E10 is CLOSED. The Director's eye ruled the true-tempo A/B on 2026-08-12,** and his
verdicts land as decision content:

1. **E10 wins the A/B.** Tempo good, not perfect. Standing direction recorded: **the more
   fps the better** — densified driving (81 frames at the trained horizon, 20.0 fps
   true-tempo delivery) is now this route's default driving format. The smoothing lever
   (filtering the motion record's own texture) stays shelved and alive — "not perfect"
   keeps its place on the chop axis for whenever his eye asks for the rest.
2. **The paint divergences his eye read as gains, not defects.** The character is in a bar,
   and a person sits in the background unasked — the populated, re-composed scene is
   *valued*. The probe ruling's confound stands as measurement (seed vs horizon,
   unattributed), but its urgency is gone: **the discriminating run lapses unspent.** The
   attribution question remains a named open item any future spec may claim — it buys
   understanding, not a fix, and nothing downstream currently needs it. E10's total spend:
   one generation; `estimate_credits` 0 throughout; the GPU bucket lands in the ledger when
   the invoice window resolves.
3. **E10 rests on E08's shoulders** — his words, and the record's shape: the route, the
   convention, the reference ruling, and the gates were E08's; E10 moved one variable on
   top of them.

Carried standing: the **two-seed rule** for scene claims (a scene property needs at least
two seeds — folded at the probe ruling, and E11 is read under it from birth); the stale-
label instrument law; the hold-frame law; the A/B builder's neither-arm-resampled rule.

`E10-density` merges to `main` at close. Suite at close: 656 passed / 46 skipped, verified
by the ruling seat's own run. **Next: E11 — the no-control route — whose dispatch hold is
released by this close.**
