# E11 wave 2 — ruling: every gate passed on a graph that could only produce noise

**Advisor ruling, 2026-08-12. E11 remains OPEN; one reserve whole.** Verified by this seat:
the suite (**795 passed / 46 skipped**, own run); branch pushed at `a952019`; the collapse
looked at with this seat's own eyes — f1 holds the figure in silhouette inside a degrading
frame with the face already unreadable, f8 is a pure orange lattice with no subject, no
scene, no camera. The report's description corroborates exactly.

## R1 — the advisor's failure, owned in full

The wave-2 amendment shipped **without a premise table** — the discipline the base spec
carried and the composed wave skipped. Its route line, "both core, weights mapped Apache,"
fused two claims and concealed a third: *that the camera tier runs on the weights the graph
already loads.* That third claim was never stated, never marked measured-or-assumed, and
never checked — by the advisor who wrote it, or anyone downstream inheriting it as settled.
The licence row this seat landed at consult #7 was for weights **the graph never loaded**.
One generation bought the lesson. Two laws land in CLAUDE.md:

- **A licence row is not a wiring claim.** Every spec that adds a conditioning node names
  the exact weight files its graph loads, as a marked premise, checked against the builder.
- **Every amendment that dispatches work carries a premise table** — composed waves doubly,
  because composition is where fused claims hide.

And one correction to the record: the executor reported "no Fun-Camera row at all" — **main
carries the row at line 43** (landed at `d96bc95`); the executor read the *branch
checkout's* stale copy. No harm — its zero-spend fetch re-confirmed Apache — and the law it
teaches is folded: **binding documents (the licence map above all) are read from `main` at
dispatch time, never from the worktree's checkout.**

## R2 — the negative stands, attributed, and the lever is untested

`WanCameraImageToVideo` fed a camera embedding to the plain I2V experts — a base with no
channel for the signal. Collapse boundary pinned: f0 clean (the round-tripped start frame),
f1 degrading with the figure still present, f2 fully collapsed; the subject survives one
generated frame in silhouette and zero in face. The catalog carries the dedicated weights
the graph should have loaded — `wan2.2_fun_camera_high_noise_14B_fp8_scaled` and
`…_low_noise_…` — and the upstream card confirms the Fun-Camera model is a derivative of
Wan2.2-I2V-A14B trained for camera synthesis. **The camera lever has not been tested;
nothing in wave 2 is evidence for or against it.**

## R3 — the instrument finding, carried furthest: Gate PAIR is commissioned

Licence, seed, frame legality, link topology, the camera/frame andon, the saved-file round
trip — **all green, on a graph that could only produce noise.** The suite checks everything
except whether the model can receive the conditioning wired at it. Commissioned, riding
wave 3's commit: **Gate PAIR** — a data-driven pairing table in `route_gates`
(conditioning class → required weight family: `WanCameraImageToVideo` → `fun_camera`;
`WanAnimateToVideo` → `animate`; `WanVaceToVideo` → `vace`; `WanImageToVideo` /
`WanFirstLastFrameToVideo` → `i2v`), raising in-tool when a graph wires a conditioning
class against an absent family. Its red test is the **exact wave-2 graph** — the gate is
not done until it goes red on the artifact that earned it.

## R4 — the executor's conduct, adopted

Halted on a stale dispatch block (wave 1's paste, re-handed — a relay error, now named)
before any spend. Two gates fired on their own holes pre-spend and were fixed rather than
skipped — the second sighting for `gate_saved_graph`, whose fail-closed refusal to skip
unknown classes is exactly why it exists. Predictions committed ahead of submission; the
honest scoring afterward — seven of twenty clauses unreadable, two "hits" disqualified as
hits on instruments with no arm to grade — is the calibration culture holding under a
negative. The empirical widget-order second reading is banked, and the zero-spend licence
fetch resolved what it believed was a blocking gap.

## R5 — wave 3 is dispatched: the corrected wave 2, on the last reserve

The Director's standing direction (camera held + performance-dominant prompt) proceeds,
corrected: **(1)** experts swap to `wan2.2_fun_camera_{high,low}_noise_14B_fp8_scaled` —
Comfy-Org repacks of the fetched Apache card; the map's repack ruling extends, and the row
gains the filenames and the card's training facts; **(2)** length moves to **81** (the
card's training horizon, and the Director's more-fps ruling agrees); **(3)** resolution is
**derived, not assumed** — the card names 512/768/1024 training resolutions; the executor
derives the nearest in-distribution frame to our aspect, records the choice and its source,
Gate L on the actual graph; **(4)** the start frame is re-authored at the chosen size
**under the alpha law** (RGBA authored; the submitted composite recorded — wave 2 was the
pinned-frame exception, and it is spent); **(5)** the wave-2 prompt surgery rides unchanged
— it is untested and its test is this run; **(6)** Gate PAIR ships first and goes red on
the wave-2 graph before wave 3's is admitted. Ceiling: the one remaining reserve. The
weights-held pin against wave 1 breaks by necessity; the comparison is route-level and says
so.

## Standing

Meters: `estimate_credits` 0 throughout; 2 of 3 spent; GPU buckets to the ledger on
resolution. E11 stays open for wave 3's report, where the Director's three verdicts —
the dance, the camera, the hands — finally get their test.
