# S03 — ruling: the kit exists, and the chain breaks exactly where it can be re-shaped

**Advisor ruling, 2026-08-13, on the S03 executor's report (`S03-run` @ `ae2fc79`, three
commits, unmerged — the branch stays unmerged until the arc closes).** Zero partner
credits across the whole run; the halt gate read "0 credits — no paid API nodes found"
and never fired. Artifacts were looked at by this seat at ruling time before anything
below was written; the kit itself goes to the Director's eye, which is the remaining
gate.

## R1 — Conduct: correct throughout, and one premise imprecision is this seat's

The lineage resolution is the model behavior: the executor hash-walked the A2 clip's
provenance to `performer_textured.glb` (`9e20ea7d…`) — E33's declared deliverable —
rather than picking from the premise row's three-GLB list, **none of which is the
canonical textured unposed asset** (two pre-texture, one posed). The no-halt was right:
a lineage continuous and byte-exact at every link is the opposite of ambiguity. The
imprecision is the dispatch's: **an enumeration in a premise row reads as an answer
key** — the E13 report's list was "GLBs on the rig," never "canonical candidates," and
the premise row carried the list without saying so. Owned; the reciprocal rule joins
the coherence-row lesson: a premise list is marked exhaustive or candidate-only at
write time.

Pre-registration integrity held, including the right handling of its own defect: P-A8
contains a forbidden word, and the executor **disclosed it rather than editing a
pre-registration after seeing results**. That is the only correct repair of a
pre-registered document. P-A4's half-miss is scored as written.

## R2 — Task A: accepted as instrument output; the Director's eye rules the kit

Eight views, alpha extrema (0, 255) on all eight; Gates ALPHA, TURN and WHOLE green;
framing within pixels of the old set; staging inherited from E09/E10 verbatim rather
than invented. The coherence row — the halt ruling's new law, its first application —
was verified at the executor's look and re-verified at this seat's own look at ruling
time: the A2 clip's performer and the kit's subject are the same ball-jointed lay
figure, same ovoid cranium and small ears, same paddle hands and oval feet, same wood
tone. What remains is genuinely the Director's: whether the darker E09/E10-staged
rendering is the reference look the composed route should hand a hosted tier, and
which views are usable given R3's persisting patches.

## R3 — Task B: the survey stands, and the deltas are attributed, not mysterious

The texture-projection patches persist exactly as the halt ruling predicted — views 0
(front) and 4 (back) clean, the other six affected, worst on the three-quarter and
profile views. Two factual deltas beside alpha, both attributable: the patches read
**grey rather than white** and the new set is **darker throughout** (interior means
70–115 against 135–167) — the staging is E09/E10's and facet's `turn_final` lighting
is not recorded on this rig. The re-coloring of the patches under different lighting
is itself confirmation they are **unpainted texture, not render artifacts**. The
survey is the evidence facet's projection-coverage arc consumes; nothing here fixes
textures, correctly.

## R4 — Task C: the mechanism is measured true, and its one link is measured broken at scale

Everything either side of the break held: 81 pinned frames reproduce the E12 `gate_b`
record exactly (under the tool's own formula — see R5); 81 uploads returned 81
distinct content-addressed names (not sha256 — each captured); `CreateVideo(fps=16)` →
`SaveVideo` produced a decodable h264 whose only delta from source is yuv420p chroma
subsampling, structured at edges (12.19 vs 5.28 by gradient) — both clauses of P-C4.
The break: `BatchImagesNode` refused dotted slot `images.image50` **at execution**,
after pre-flight accepted the same graph with zero warnings, while the identical
8-frame chain executes end to end. Ruled: **a runtime slot cap, inferred and honestly
unmeasured** — the catalog declares no maximum and no boundary-hunting submission was
made, correctly, because the dispatch scoped no re-runs.

**The A2 rescue re-shapes rather than dies:** cascade batching — batch the batches —
is the named follow-up, zero partner credits, entering through a spec amendment and
not through improvisation. The r2v-acceptance link remains **ASSUMED**, in those words,
untouchable at zero credits.

## R5 — Instruments and doctrine off this run

- **The median-formula trap, now doctrine:** `gate_b_frames` computes
  `sorted(deltas)[n//2]` — the upper-middle element — where numpy's median averages
  the middle pair. Pin verification uses the recording tool's own formula, or a
  perfectly intact pin reads as corruption. The executor caught this by refusing to
  report a discrepancy either way before locating its source.
- **Pre-flight green then execution failure** — the cleanest sighting yet of the
  standing law that a `dry_run` PASS does not prove link sanity: this graph was
  round-trip-admitted, Gate-ROUTE-walked, pre-flight-accepted, and cannot execute at
  81 slots.
- **The worktree suite skew reproduces at the same size** (1015/48 here — S03 adds 45
  tests — against main's 1005/13; the same 35-test gap E13 flagged). Twice-sighted,
  still unexplained, still asserted-nothing. The instrument question stands on the
  handoff.

## R6 — What this unblocks

E13's re-arm now waits on **exactly one thing: the Director's eye on the kit.** On his
pass: the E13 amendment sets A1 from the kit's usable views; A2 either waits on the
cascade-batch probe (zero credits, small, spec'd not improvised) or the re-arm goes
stills-only on his word. If his eye rules the lighting wrong for reference use, the
kit re-renders under a recorded staging choice — the instrument is now one command,
which is what S03 was for.
