# S05 — closing ruling

**Advisor seat, 2026-08-13.** The report is [S05-report.md](S05-report.md) (`S05-run`,
three commits, branch CI green, merged at this close). The sheet and the tight arm's
one rendered cell went to the Director's eye at full size; this seat opened both before
describing either. Zero credits, zero cloud calls; one andon fired, on the arm the spec
wrote for a raise, and nothing was improvised past it.

## R1 — Verdicts

**H-S05d, the Director's clause: green.** The sheet reads as intended at his eye — the
same figure, the same frame, smaller by exactly the pinned ratio; the roster relation
made visible on one subject. His word.

The mechanical hypotheses: **H-S05a held** — arm SOLVED reproduces S04 bit-for-bit,
the scale to the last digit, every bbox, every clearance, the same worst delta.
**H-S05b held on all three clauses** — 8/8 green, CROP silent, and the largest view at
684 px exactly. **H-S05c split, and the miss is this seat's:** the arm's design clause
held (an andon raised on the first rendered view, the run halted there, the raise
treated as the arm's result), but the attribution clause missed — **Gate WHOLE fired,
not Gate CROP**. The executor's P-3b, pre-registered from arithmetic on S04's published
table before any render, is **confirmed against the spec**.

## R2 — The attribution miss, graded, and the doctrine it buys

The spec reused S04's conclusion — CROP is the andon on the unbounded direction —
after removing the premise that conclusion stood on. S04's ruling said Gate WHOLE
passes by construction *because the solve fits the very projection WHOLE reads*. **A
pin is fitted to nothing.** Removing the solve un-blinded WHOLE's height axis, and
WHOLE sits upstream, so it reported first — on a cell that was genuinely cropped
(rendered bottom clearance 0, CROP's own raising condition; both andons live, the
upstream one reporting).

Graded: **spec MISS, executor HIT** — the second consecutive two-seat disagreement
resolved for the executor (E14: the identity arm; S05: the andon attribution). The
shared failure shape sits in the spec seat both times: **reasoning from a prior
conclusion whose premise the new experiment removes.** The doctrine line, now written:
*when a spec removes a premise, every passes-by-construction conclusion resting on it
re-enters play — enumerate the andon chain from the changed premise, not from the last
ruling's conclusions.*

**The gate placement itself is ruled correct as it stands.** Two gates, two objects —
WHOLE reads the projected cloud, CROP reads the rendered alpha — and proper layering
puts the upstream reader first. The pair protected the run; which member names a
failure is layering, not protection. No re-ordering, no merge of the two.

## R3 — The decimation-gap question: no further arm

Ruled: **Gate CROP owes no real-render demonstration.** Its anti-vacuity stands on its
red tests at all four borders at the inclusive last index, and on the tight cell itself,
which measured CROP's own raising condition on a real render even though WHOLE reported
first. The band where CROP would report alone is the decimation gap — ≤ 7.93 px against
62–86 px of clearance on this subject, one to two percent of pin, subject-dependent.
Tuning a pin into a seven-pixel band so a specific gate gets to be the reporter is an
experiment about the reporter, not the protection. Closed.

## R4 — The remaining surfaced items, ruled

1. **`--height-frac` beside `--ortho-scale`, accepted with disclosure — ratified.** The
   manifest's `height_frac_participates: false` is the protection; a parser refusal
   needs a sentinel default and buys nothing the record does not already give.
2. **The `--mode=scale` deviation — ratified.** Compare mode's positional `PERSP` tag
   printed over an ortho row is the S04 disease — a literal that lies when reused, on a
   sheet that looks finished — refused this time before it shipped. Compare mode itself
   untouched; the full-precision `repr` in row titles is right, because on a pinned row
   the number is the recipe.
3. **The `-O` probe's second `except` — ratified.** Widening the existing catch to
   `ArmatureError` would have turned every `WRONG_ERROR` outcome already in it into a
   silent pass; a real weakening, avoided by enumeration.
4. **The conftest fixture move — ratified**; the pin tests share S04's bpy stub and
   `test_turnaround_ortho.py` still collects 40.
5. **The one-ULP pre-registration defect: disclosed, unedited — conduct ratified**, the
   third application of S03's precedent. Same hand-arithmetic family as S04's P-C3;
   advisory note to future executor seats: where a pre-registered number is defined as
   a machine product, derive it with the machine. Advisory only — predictions are
   allowed to be wrong; that is what makes them predictions.
6. **`TurnaroundPlanRefusal` as a refusal, not a gate letter — ratified.** It refuses
   before a camera exists; the gate vocabulary stays reserved for measured views. The
   second refusal site is not redundant: the plan is the documented branch object, and
   a pin silently dropped there renders a roster on per-character scales with every
   gate green — the exact failure the pin exists to prevent.

## R5 — Conduct

Twenty of twenty-one prediction clauses hit, with the honest header that most were
derivation from S04's published numbers rather than foresight — the scoring read the
way the executor said to read it. The stale watchdog heartbeat was cleared and re-armed
before any GPU work. The venv disclosure, the sixteen-distinct-digests check on TURN,
and the free wall-clock finding (a pinned run is 3.38 s faster — the skipped solve's
pure-Python cost) all ride the record. `math.isfinite` was checked rather than assumed,
and the manifest defect the natural non-edit would have shipped — a pinned run wearing
a solve record naming a `height_frac` it never targeted — is now structurally
untestable to ship, with the red test asserting the value appears nowhere. Standards
self-scores accepted, including the honest 2 on EXTERNAL_VERIFIER.

## R6 — Close mechanics and the standing board

`S05-run` merges at this close; the suite is re-measured on main at the merge and the
number rides the merge-side commits. The board after this close, each item carrying
the word it already has:

1. **The roster pin is ready for a real cast.** The first multi-character roster names
   its own spec when a second canonical character exists; the pin's mechanics are
   proven single-subject.
2. **The shot-set re-proof on the repaired performer** — gated on facet's
   projection-coverage arc (the studio's standing top priority). H-S04c and the 51-row
   placement question grade there, by the Director's standing word.
3. **The E14 follow-ons** — the T strength sweep, the subject-scoped prompt probe, the
   S low-strength candidate — wait on the Director's word, unchanged.
