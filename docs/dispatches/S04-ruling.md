# S04 — closing ruling

**Advisor seat, 2026-08-13.** The report is [S04-report.md](S04-report.md) (`S04-run`,
four commits, branch CI green, merged at this close). The sheets went to the Director's
eye at full size before this ruling; the profile cells were re-opened at full size by
this seat beside S03's survey panels before any seam claim below was written. Zero
credits, zero cloud calls, no gate fired in the run.

## R1 — The instrument stands; the eye's verdict is split by the Director's own word

The mechanical hypotheses resolved by gate record: **H-S04a held** (8/8 ortho views,
Gate ALPHA green) and **H-S04b held** (Gate CROP silent on all eight under the one
shared scale, tightest clearance 62 px). The spec's one ASSUMED premise is now a
permanent measured fixture: Blender 5.2 accepts and honours ORTHO + `ortho_scale`, the
scale spans the longer axis, and `Image.pixels` is bottom-up — the convention that,
wrong, would have made Gate CROP name the wrong border in every report it ever wrote.

**H-S04c takes no verdict today, by the Director's disposition:** the shot-set proof
re-runs on the repaired performer once facet's projection-coverage arc lands, and the
sprite-cell verdict is graded there. Per the E14 law, no seat grades it in the
meantime; the deferral is recorded as his word, not a seat's reading.

What his eye did rule from these cells: **the pale seam along the profile views is the
pre-known texture-hole defect** — verified this session by opening the S04 ortho
profile cell beside S03's RGBA-true survey panel: identical patches at identical
landmarks (jaw, ear, neck, shoulder, flank, knee), two instruments, two dates, one
texture. The mesh is good; the paint carries unpainted strips at the projection-coverage
boundary, worst on lateral views, clean front and back — facet's top-priority arc,
exhibit count now one higher, and no finding of S04's.

## R2 — The four surfaced items, ruled

1. **Gate CROP arms on the ortho path only — correct as built.** Task A's
   untouched-perspective invariant outranks symmetric gating, and the perspective
   clearances ride the manifest so this stays decidable without a re-run. The first
   follow-on vindicates the choice: S05's roster pin lands its failure mode exactly
   where CROP already stands guard. If perspective framing ever gains a solve-derived
   scale, CROP arms there through that change's own spec.
2. **The perspective-side manifest key (`projection: "PERSP"`) — ratified.** A manifest
   that does not name its projection is the worse artifact; the key changes no pixel;
   flagged-rather-than-assumed was the right conduct.
3. **P-T6's margin — recorded as a diagnostic observation, not a claim.** Direction as
   predicted, one run, no noise floor measured. The harder test the executor named — a
   subject with more depth variation than a near-axisymmetric lay figure — is a
   candidate future measurement, unscheduled.
4. **The 51-row placement difference — measured, mechanically explained, and deferred
   with H-S04c** to the repaired-performer re-proof. It stands recorded for sheet
   design meanwhile: parallel projection carries no elevation term, so the ortho set
   seats the figure ~51 rows lower at 30°.

## R3 — Predictions and conduct

The executor's scoring table is accepted as written — including both misses kept as
misses. **P-T3b's structured split is a small banked finding:** the measured-wider
pattern holds on all four 45° views and fails on all four axis-aligned ones, where the
decimated cloud's world-axis extremes *are* the screen extremes, so decimation cost
vanishes and anti-aliasing pulls the thresholded silhouette inward instead. P-T7's
~30× wall-clock miss prices ortho rendering far cheaper than predicted — 0.88 s per
view. P-C3's numeric sub-clause miss was hand arithmetic, owned as such.

**The forbidden word in the pre-registration: disclosed, unedited — conduct ratified**,
the second application of S03's precedent. Editing a pre-registered document after
seeing results is the thing pre-registration exists to prevent; disclosure is the only
correct repair. The mid-run corrections — the flip-blind delta claim in the first
commit's message, and the replacement probe that failed its own red test before
reaching the tree — were owned in the record rather than smoothed, and what shipped is
the testable decomposition plus the calibration.

**Enumeration paid twice**, on the record: `mask_bbox` reused instead of a second bbox
implementation; `sheet_compose` reused for layout with only cell treatment new. The
standards self-scores are accepted, including the honest 2 on EXTERNAL_VERIFIER.

## R4 — The worktree skip-skew closes: cause located and measured

Third sighting, now settled. Measured at ruling time: the main checkout's `outputs/`
carries the historical, gitignored E01–E07 run artifacts; every worktree carries only
its own runs. All 35 extra skips sit in three files whose skip reasons name exactly
those artifacts ("E02 payloads are gitignored output", "E03 upload records…"). Gitignored
artifacts do not travel with branches, so tests requiring them skip precisely where
they are absent — **which is the suite's stated design working** ("rig-local assets
skip visibly"). No fix is owed; the handoff's half-answered instrument item retires.
main's 13 skips remain the strict subset that lack rig-local assets everywhere.

## R5 — Close mechanics and what stands next

`S04-run` merges at this close; the suite is re-measured on main at the merge and the
number rides the merge-side commits, not this ruling. Next, each already carrying the
Director's word from today:

1. **S05 — the roster scale pin** dispatches now: an explicit `--ortho-scale` override
   so a roster renders on one common world scale and relative character heights
   survive into the sheet, with Gate CROP refusing any pin too tight for its subject.
2. **The shot-set re-proof on the repaired performer** — gated on facet's
   projection-coverage arc (the studio's standing top priority; its advisor paste
   block is already in the Director's hands). H-S04c and the placement question grade
   there.
