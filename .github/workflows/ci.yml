# Studio Actions rules binding: ubuntu-latest only, paths-gated (never "all
# pushes"), workflow_dispatch fallback, and the required concurrency block.
#
# Pages deployment is NO LONGER an open question — it was ruled and it lives in
# pages.yml. See docs/dispatches/S01-ruling.md §2 for the measurement (71 of 71
# deploying repos in this org use a workflow build) and why a third workflow
# file is correct here despite the two-file cap.
#
# `release.yml` stays reserved for the PyPI Trusted Publisher: OIDC
# authenticates the workflow FILENAME (docs/publishing.md).
name: ci

on:
  push:
    paths:
      - "site/**"
      - "tools/**"
      - "tests/**"
      - ".github/workflows/**"
      # The packaging manifests. Added at v0.2.0: without these a change to
      # pyproject.toml or the launcher could reach a tag having never been built,
      # and the first place that failure would surface is the release job — after
      # the tag exists and while a registry is waiting.
      #
      # README.pypi.md and LICENSE are the SAME failure, left open at v0.2.0 and
      # measured since: pyproject names both (`readme` and `license.file`), the
      # sdist ships both, and the wheel METADATA carries the licence text and the
      # long description from them. Neither matched any filter, so renaming or
      # deleting one triggered no CI run at all. Every file the package is built
      # from now builds the package; tests/test_ci_workflows.py reads the list out
      # of pyproject, so a rename moves the requirement with it.
      - "pyproject.toml"
      - "npm/**"
      - "README.pypi.md"
      - "LICENSE"
  pull_request:
    paths:
      - "site/**"
      - "tools/**"
      - "tests/**"
      - ".github/workflows/**"
      # The packaging manifests. Added at v0.2.0: without these a change to
      # pyproject.toml or the launcher could reach a tag having never been built,
      # and the first place that failure would surface is the release job — after
      # the tag exists and while a registry is waiting.
      #
      # README.pypi.md and LICENSE are the SAME failure, left open at v0.2.0 and
      # measured since: pyproject names both (`readme` and `license.file`), the
      # sdist ships both, and the wheel METADATA carries the licence text and the
      # long description from them. Neither matched any filter, so renaming or
      # deleting one triggered no CI run at all. Every file the package is built
      # from now builds the package; tests/test_ci_workflows.py reads the list out
      # of pyproject, so a rename moves the requirement with it.
      - "pyproject.toml"
      - "npm/**"
      - "README.pypi.md"
      - "LICENSE"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Pull-request only, deliberately. On a push to main, pages.yml already builds
  # site/ before deploying it, so running this job there would burn a second
  # build of identical bytes for no coverage.
  site-build:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # `npm ci` and not `npm install`: the lockfile is committed, and a build
      # that resolves fresh dependencies is not the build that was verified on
      # the rig. If this step fails on a lockfile mismatch, that IS the signal.
      - name: install site dependencies
        working-directory: site
        run: npm ci

      # The repo's only dependency manifest is site/. There is no Python manifest
      # here — the test dependencies are the pinned install line below and nothing
      # resolves a graph — so this is the whole scannable surface, and it is scanned
      # rather than attested. `high` is the studio's threshold; a build-time low is
      # not worth a red main.
      - name: scan site dependencies
        working-directory: site
        run: npm audit --audit-level=high

      - name: build site
        working-directory: site
        run: npm run build

  # E01's suite. The Blender-dependent tests carry
  # `pytest.mark.skipif(not os.path.isfile(BLENDER))`, so on a runner with no
  # Blender they SKIP VISIBLY rather than passing silently — the skip count in
  # the log is the honest record of what this runner could not exercise.
  # Blender-side coverage is the rig's job, not CI's.
  python-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      # opencv is pinned to the rig's verified version: the aapose golden-frame
      # tests assert byte-stable rasterization, and the rasterizer's version is
      # the axis that matters. Headless build — no GUI stack on a runner.
      - name: install test dependencies
        run: python -m pip install --upgrade pip numpy pillow pytest opencv-python-headless==5.0.0.93 matplotlib==3.11.1

      - name: run tests
        run: python -m pytest tests -q

      # The ANDON tests assert that G1 still raises under `python -O` and
      # PYTHONOPTIMIZE=1. Running the suite a second time under -O is what
      # proves the gate is not an `assert` the interpreter is licensed to
      # delete — 87 of facet's ANDONs were removable by an environment
      # variable, and this repo does not add the 88th.
      - name: run tests under -O (gates must still raise)
        run: python -O -m pytest tests -q
        env:
          PYTHONOPTIMIZE: "1"

      # The package must build and install on every change that could break it, not
      # first at release time. The install is into a CLEAN venv and the command is run
      # from THAT install rather than from the checkout — otherwise a module missing
      # from the wheel still passes, because the source tree is sitting right there.
      #
      # `armature check` and `armature modules` were the whole leg, and they execute no
      # function body: `check` imports each surface module and `modules` prints a table, so
      # no lazy import is ever reached. Measured in this exact clean room — armature-studio
      # and numpy only — `armature check` printed `all modules resolved` and exited 0 while
      # cv2, matplotlib and PIL were absent and both the drawing path and Gate DONOR raised
      # ModuleNotFoundError on first call. The leg was green on precisely the wheel defect it
      # exists to catch. The probe below CALLS through to each function-local dependency, and
      # tests/test_ci_workflows.py derives the required call sites from the source, so a new
      # lazy dependency fails until this probe reaches it.
      - name: build the package and run it from a clean install
        run: |
          python -m pip install --upgrade build twine
          python -m build
          python -m twine check dist/*
          python -m venv /tmp/cleanroom
          /tmp/cleanroom/bin/python -m pip install --quiet dist/*.whl
          /tmp/cleanroom/bin/armature check
          /tmp/cleanroom/bin/armature modules --json > /dev/null
          /tmp/cleanroom/bin/python - <<'PROBE'
          import os, tempfile
          import numpy as np
          from armature_core import aapose, donor_gate, pngio

          # The leg's own premise, checked rather than assumed: this must be the wheel, not
          # the checkout sitting one directory up.
          if "site-packages" not in aapose.__file__.replace("\\", "/"):
              raise SystemExit("clean-room probe imported the source tree: " + aapose.__file__)

          canvas = aapose.blank_canvas(64, 64)
          body = np.zeros((aapose.KEYPOINT_COUNT, 3)); body[:, :2] = 32.0; body[:, 2] = 1.0
          aapose.draw_body(canvas, body)
          hand = np.zeros((aapose.HAND_KEYPOINT_COUNT, 3)); hand[:, :2] = 32.0; hand[:, 2] = 1.0
          aapose.draw_hand(canvas, hand)

          frames = tempfile.mkdtemp()
          paths = []
          for i in range(2):
              p = os.path.join(frames, "%05d.png" % i)
              pngio.write_png(p, np.full((8, 8, 3), i * 40, dtype=np.uint8))
              paths.append(p)
          donor_gate.mean_consecutive_frame_difference(paths)
          print("clean room: draw_body, draw_hand and mean_consecutive_frame_difference all ran")
          PROBE

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: launcher self-test
        working-directory: npm
        run: npm test
